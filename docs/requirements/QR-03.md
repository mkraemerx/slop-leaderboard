# QR-03 Portability

The application runs as a single Docker container with no external services beyond the platform API (GitHub/GitLab). All persistent data lives under a single directory that can be bind-mounted.
