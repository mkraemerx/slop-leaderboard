# QR-02 Reliability

Pending analysis jobs must survive application restarts without data loss or duplicate processing.

**Acceptance criteria:**

- A job that was queued or running when the application stopped is re-executed after restart
- Restarting mid-analysis does not produce duplicate commit records
