# FR-09 UI

The application is a server-rendered web UI with progressive HTMX enhancements.

**Acceptance criteria:**

- Distinct pages exist for: leaderboard, exercise list, solution comparison, and fork management
- Filter and scope changes update only the relevant section of the page
- Each fork shows its current sync state (pending / running / ok / error)
- All functionality works without JavaScript enabled
