# FR-08 Webhook

Forks can be configured to send push webhooks, triggering an immediate incremental analysis.

**Acceptance criteria:**

- A valid push webhook for a tracked fork triggers an incremental analysis right away
- When a webhook secret is set, deliveries with an invalid or missing signature are rejected and no analysis is enqueued
