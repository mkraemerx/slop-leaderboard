# QR-04 Maintainability

Business logic, persistence, scheduling, and web handling are separated into distinct modules with clear boundaries. SQL queries are written explicitly; no ORM abstraction layer.
